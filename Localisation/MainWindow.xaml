<Window x:Class="Localisation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Localisation"
        xmlns:loc="clr-namespace:Localisation.Loc"
        mc:Ignorable="d"
        Title="MainWindow" Height="244.041" Width="438.935">
    <Window.Resources>
        <local:EnumToStringConverter x:Key="converter"/>
    </Window.Resources>
    
    <StackPanel>
        <Button Content="{x:Static loc:Resource.Btn_String}" Click="Button_Click"/>
        <TextBlock Text="{x:Static loc:Resource.Tbl_String}"/>

        <ComboBox ItemsSource="{local:Enumerate local:TestEnum}"
                  SelectedItem="{Binding SelecedEnumValue, Converter={StaticResource converter}}"/>
        <TextBlock Text="{Binding SelecedEnumValue}"/>
    </StackPanel>
</Window>
